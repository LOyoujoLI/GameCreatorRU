## Start

First let's analyze the requirements.

There are 60 seconds in 1 minute, all of which are constantly +1 to determine during the calculation.

1 second... .2 seconds... ...3 seconds, so we can achieve this effect by adding numeric variables in a loop.

And how to do 60 seconds is 1 minute? We can also use the mathematical calculation of numeric variables to achieve this effect.

## Get seconds

Let's create a variable that is used to get the seconds, which is simply used to get the seconds.

![](img/In-depth/010/001.png)

Create another variable to calculate the seconds, the purpose of this is so that when the calculation is done later on, the fetching of the seconds is correct and will not be affected by the calculation.

![](img/In-depth/010/002.png)

## Calculation

1 minute is 60 seconds, so here we can use division to achieve this effect.

![](img/In-depth/010/003.png)

### Caution

1、the calculation process will inevitably result in a decimal point, so here we check **take the whole **.

![](img/In-depth/010/004.png)

2、if the implementation of the loop leads to other Command can not be properly executed, you need to consider whether it is accounted for the process, if so remember to check **independent event**.

![](img/In-depth/010/005.png)

### Test

In the test we found that although there is 1 minute for 60 seconds, 60 seconds does not go to zero, so next we have to consider changing to 0 when the seconds are 60.

![](img/In-depth/010/006.gif)

## Return to 0

We can use remainder to change 60 to 0.

![](img/In-depth/010/007.png)


![](img/In-depth/010/008.png)

## Test

![](img/In-depth/010/009.gif)
