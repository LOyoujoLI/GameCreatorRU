# Code example: scene object related
Scene object includes scene object data and object entity. Scene object data is mainly used for storage reading and transmission, which generally refers to SceneObject, and also includes custom properties of scene object.<br>
Scene objects generally occupy the list of scene objects in the scene in a placeholder manner (scene.sceneObjects).<br>
Scene objects that belong to a player can find the corresponding player by so.player.<br>
Instances of scene objects are created based on their prototype binding to the scene object class.<br>
[Concept: Scene object](?file=003-GC本体文档/0020-场景对象/001-概述)<br>
[API-Generic - Scene Object Base Class:SceneObject](?file=007-API-2D网络内核/001-通用/024-SceneObject)<br>
[API-General - Scene Object Entity Class:SceneObjectEntity](?file=007-API-2D网络内核/001-通用/026-SceneObjectEntity)<br>
[API-Client-Scene Object Class:ClientSceneObject](?file=007-API-2D网络内核/002-客户端/019-ClientSceneObject)<br>
[API-Server-Scene object class:ServerSceneObject](?file=007-API-2D网络内核/004-服务端/015-ServerSceneObject)<br>

# Client scenario objects
#### Standalone version: Create scene objects
**Create objects by cloning an object in a scene:**<br>
The properties of the cloned object are identical to the pre-defined source object.<br>
>var fromSceneID = 1; // ID of the corresponding scene<br>
>var sceneObjectID = 5; // The ID of the pre-positioned scene object in the scene<br>
>var soc: ClientSceneObject = Game.currentScene.addSceneObjectFromClone(fromSceneID, sceneObjectID, true) as any;<br>

**Create objects by New**<br>
New object properties need to be filled by themselves, custom properties also need to be filled by themselves, if the custom properties and display objects are many and complex, it is recommended to use the cloning method, cloning and then modify the necessary properties can be.<br>
>// New scene object data<br>
>var so = new SceneObject();<br>
>// Set the basic properties of the scene object:<br>
>so.avatarID = 1; // avatar set to avatar #1<br>
>so.avatarAct = 1; // Play #1 action<br>
>so.modelID = 1; // Using prototype #1<br>
>// Add to scene object, create entity object from data, return value is entity scene object<br>
>var soc = Game.currentScene.addSceneObject(so,false,true);<br>
>soc.x = 500;<br>
>soc.y = 500;<br>
>soc.Custom Properties = xxx;<br>

### Standalone version: Remove scene objects
Default scene objects are automatically removed from the scene if they do not meet the occurrence conditions, but are not removed from the record list (scene.sceneObjects).
**Remove from the current scene and remove from the record list**<br>
>// so is the scene object data or entity, find the entity object at that location by so.index in order to remove<br>
>Game.currentScene.removeSceneObject(so, true):

**Remove from the current scene, but not from the list of records**
>//  so is the scene object data or entity, find the entity object at that location by so.index in order to remove it<br>
>Game.currentScene.removeSceneObject(so, false):

#### General: Play and unplay animations on scene objects
Animation played on the scene object:
> playAnimation(aniID: number, loop: boolean, isHit: boolean, fps: number = null, superposition: boolean = false): Animation

Stop playing the animation on the object
> stopAnimation(aniID: any): void;



#### Network version: Create scene objects
The network version of the scene object data is on the server side, and the client scene scene object is generally created from the server after the scene object data is transferred to the client.<br>
You can refer to the GameCreator2DNet kernel project. After logging in and connecting to the server, the server will transfer relevant scene object data for creation, and you can set permissions when customizing properties in order to make, for example, which properties are public and which properties are private.


# Server-side scenario objects
**Creating objects using cloning**：Since the scene object may have many custom properties that need to be configured, it is recommended to use this method to create and then modify the necessary properties
> // Static methods of the ServerSceneObject class：<br>
> static clone(fromSceneID: number, fromSceneObjectindex: number, isCopy: boolean = true): ServerSceneObject

**Creating objects using the New method**：Since the scene object may have many custom properties that need to be configured, use cloning to create it whenever possible
> // ServerSceneObject Static methods of classes：<br>
> static create(soData: SceneObject, presetCustomAttrs: { [varName: string]: { varType: number, value: any, copy: boolean } } = null, player: ServerPlayer = null, isClone: boolean = true): ServerSceneObject {

# Customizing object behavior
Object behavior is customizable and must be inherited from the SceneObjectBehaviors class to implement specific logic.<br>
For example, the GameSceneObjectBehaviors class is used in the "ARPG - Book of black" game template to implement the specific behavior of the object.<br>
> class GameSceneObjectBehaviors extends SceneObjectBehaviors

Implement a logical pause state determination: in order to determine which actions are required to wait for the end before continuing. (For example, in an RPG game, if the object is moving, wait until it has finished moving before performing the next step)
>protected get logicPause(): boolean {<br>
>&nbsp;&nbsp;&nbsp;&nbsp;// to do
>}<br>

Implement the reset function: so that the behavior editor can be reset when previewing and jumping to steps<br>
>reset(defSceneObejct: SceneObject): void {}

Implement specific behaviors, corresponding to the custom behavior number, #1 custom behavior then execute behavior1, #2 then execute behavior2, and so on.<br>
And the panel properties in the custom behavior automatically correspond to the parameters, which are loaded in order<br>
It is also necessary to implement an "ignore process" for the behavior editor preview（this.ignoreProcess,The reason is that when previewing in the editor, you need to skip the process and preview the result directly, such as the act of moving to a certain coordinate.）<br>
> // For example, three parameters x, y, and whether to use grid coordinates are provided in the custom behavior editor for the user to enter.<br>
> private behavior1( x: number, y: number,useGrid: boolean) {}

<img src="img/t78.png" /><br>

In addition to this approach, it can also be implemented using a separate custom behavior, see【[Customized Behavior](?file=003-GC本体文档/1010-自定义编辑器/035-自定义行为)】

In addition, the behavior of the web version is based on the server's behavior instance, and the client is generally only available for preview by the behavior editor.

### Reference:
GameCreator2D Kernel Project<br>
GameCreator2DNet Kernel Project<br>
[API-Standalone version - Scene object base class:SceneObject](?file=006-API-2D单机内核/001-通用/024-SceneObject)<br>
[API-Standalone version - Scene object entity class:SceneObjectEntity](?file=006-API-2D单机内核/001-通用/026-SceneObjectEntity)<br>
[API-Standalone version - Scene object behavior base class:SceneObjectBehaviors](?file=006-API-2D单机内核/001-通用/025-SceneObjectBehaviors)<br>
[API-Standalone version - Client-side scenario object class:ClientSceneObject](?file=006-API-2D单机内核/002-客户端/018-ClientSceneObject)<br>
[API-Network version - Scene object base class:SceneObject](?file=007-API-2D网络内核/001-通用/024-SceneObject)<br>
[API-Network version - Scene object entity class:SceneObjectEntity](?file=007-API-2D网络内核/001-通用/026-SceneObjectEntity)<br>
[API-Network version - Scene object behavior base class:SceneObjectBehaviors](?file=007-API-2D网络内核/001-通用/025-SceneObjectBehaviors)<br>
[API-Network version - Client Scene Object Class:ClientSceneObject](?file=007-API-2D网络内核/002-客户端/019-ClientSceneObject)<br>
[API-Network version - Server-side Scenario Object Class:ServerSceneObject](?file=007-API-2D网络内核/004-服务端/015-ServerSceneObject)<br>
