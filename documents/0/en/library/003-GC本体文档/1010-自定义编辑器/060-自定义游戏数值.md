# Customized game values
Custom game values are generally used to make values related to that game (template). <br> 
For example, the number of gold coins a player has,<br> 
which may vary from item to item (template). <br> 
This value can be used to record it using variables, or to judge it, or to display it directly in the interface [[Game Value Component](?file=003-GC本体文档/0070-界面/009-游戏数值组件)]. <br> 
From a programmatic point of view, a custom game value is equivalent to a function that can take in parameters and return a numeric value as a result. <br> 
> For example, "RPG - Feather of cyan" provides some custom game values to get, determine, and display

<img src="img/2/自定义/3.png" /><br>

## Open the custom game value editor
Menu -> Custom Editor -> Game Value Settings<br>

## Edit custom game values screen
The methods for making the editor interface visually in the custom editor are the same, refer to [[Custom Editor Panel](?file=003-GC本体文档/1010-自定义编辑器/003-自定义编辑面板)]。

## Implementing game values
>module CumtomDataDisplay {<br>
&nbsp;&nbsp;/**<br>
&nbsp;&nbsp;&nbsp;* Custom value display 1 1 means corresponding to the number 1 custom game value<br>
&nbsp;&nbsp;&nbsp;* @param trigger Event triggers, which may be empty, exist when called in event instructions such as [Conditional Divergence] or [Value Set]<br>
&nbsp;&nbsp;&nbsp;* @param p Customized numeric parameters Automatically generated based on the preset parameters in the editor<br>
&nbsp;&nbsp;&nbsp;*/<br>
&nbsp;&nbsp;export function f1(trigger: CommandTrigger, p: CustomGameNumberParams_1): number {<br>
&nbsp;&nbsp;&nbsp;&nbsp;return 0;<br>
&nbsp;&nbsp;}<br>
}
