# Place to move
Use this command to switch game scenes. <br> 
Switching a game scene stops and destroys all triggers for scene events in that scene and triggers whose triggers or executors are non-player scene objects.

## Area Description
<img src="img/2/事件/7-6.png" /><br>
<table>
 <tr>
    <td><b>Regions</b></td> <td><b>Description</b></td>
 </tr>
 <tr>
    <td>Specify scene and location using variables</td> <td>With variables checked you can specify the scene and coordinates to go to based on the current value of the numeric variable<br>Scene: use the current value of the numeric variable as the scene ID<br>Scene pixel coordinate x: use the current value of the numeric variable as the coordinate to go to the horizontal pixel<br>Scene pixel coordinate y: use the current value of the numeric variable as the coordinate to go to the vertical pixel</td>
 </tr>
 <tr>
    <td>Scene List</td>
    <td>
     In normal mode, you need to select a specified scene, left mouse click to select
    </td>
 </tr>
 <tr>
    <td>Preview and assigned location</td>
    <td>
      Zoom view: use mouse wheel to zoom view, shortcut Z to restore to 100%<br> 
      Move view: right mouse button to drag the map or hold space + left mouse button to drag the map<br> 
      Determine the arrival point position: left mouse button to click to determine the position, hold SHIFT to fix at the center of the grid<br> 
      Move the arrival point position: keyboard arrow keys to move the determined position by pixel position, hold SHIFT to move by grid
    </td>
 </tr>
 </table>

## Script: Dispatch Event
The following events are dispatched when moving places, and usually the project/template is based on this to implement functions like unloading scenes and loading scenes.<br>
<img src="img/2/事件/7-7.png" /><br>
